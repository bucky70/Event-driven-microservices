spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bucky70/spring-cloud-cpnfig-server-repository.git
          default-label: main
          username: bucky70
          password:
          clone-on-start: true
    fail-fast: true